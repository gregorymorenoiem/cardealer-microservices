name: CI/CD - Tests & Deployment

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  NODE_VERSION: "20.x"
  DOTNET_VERSION: "8.0.x"

jobs:
  # ==========================================
  # JOB 1: Backend Tests (.NET Microservices)
  # ==========================================
  backend-tests:
    name: Backend Tests (.NET 8)
    runs-on: ubuntu-latest

    strategy:
      matrix:
        service:
          - AuthService
          - ErrorService
          - NotificationService
          - VehiclesSaleService
          - UserService
          - RoleService
          - MediaService
          - BillingService
          - Gateway

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        working-directory: backend/${{ matrix.service }}
        run: dotnet restore

      - name: Build
        working-directory: backend/${{ matrix.service }}
        run: dotnet build --no-restore --configuration Release

      - name: Run tests
        working-directory: backend/${{ matrix.service }}
        run: |
          if [ -d "${{ matrix.service }}.Tests" ]; then
            dotnet test --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"
          else
            echo "No tests found for ${{ matrix.service }}"
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results-${{ matrix.service }}
          path: backend/${{ matrix.service }}/**/*.trx

  # ==========================================
  # JOB 2: Frontend Unit Tests (Vitest)
  # ==========================================
  frontend-unit-tests:
    name: Frontend Unit Tests (Vitest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        working-directory: frontend/web-next
        run: pnpm install --frozen-lockfile

      - name: Run unit tests
        working-directory: frontend/web-next
        run: pnpm test -- --run --reporter=verbose
        continue-on-error: true

      - name: Generate coverage report
        working-directory: frontend/web-next
        run: pnpm test:coverage -- --run
        continue-on-error: true

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: frontend/web-next/coverage/coverage-final.json
          flags: frontend-unit
          name: frontend-unit-tests
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-unit-test-results
          path: frontend/web-next/coverage/

  # ==========================================
  # JOB 3: Frontend E2E Tests (Playwright)
  # ==========================================
  frontend-e2e-tests:
    name: Frontend E2E Tests (Playwright)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        working-directory: frontend/web-next
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Browsers
        working-directory: frontend/web-next
        run: pnpm exec playwright install --with-deps chromium

      - name: Run E2E tests
        working-directory: frontend/web-next
        run: pnpm test:e2e -- --project=chromium
        continue-on-error: true

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/web-next/playwright-report/
          retention-days: 30

  # ==========================================
  # JOB 4: Lint & Code Quality
  # ==========================================
  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        working-directory: frontend/web-next
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        working-directory: frontend/web-next
        run: pnpm lint

      - name: Check TypeScript types
        working-directory: frontend/web-next
        run: pnpm typecheck

  # ==========================================
  # JOB 5: Docker Build (Backend Services)
  # ==========================================
  docker-build:
    name: Docker Build - ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: [backend-tests]

    strategy:
      matrix:
        service:
          - AuthService
          - ErrorService
          - NotificationService
          - VehiclesSaleService
          - UserService
          - RoleService
          - Gateway

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/${{ matrix.service }}/Dockerfile
          push: false
          tags: okla-${{ matrix.service }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ==========================================
  # JOB 6: Integration Tests (Docker Compose)
  # ==========================================
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [docker-build]
    # Skip integration tests by default — enable manually if needed
    if: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start services with Docker Compose
        run: |
          docker compose up -d redis rabbitmq postgres
          sleep 15
          docker compose up -d authservice errorservice
          sleep 30

      - name: Wait for services to be healthy
        run: |
          echo "Waiting for services to start..."
          docker ps
          # Health check on port 8080 (K8s standard)
          curl -f http://localhost:15101/health || echo "AuthService not yet healthy"
          curl -f http://localhost:15108/health || echo "ErrorService not yet healthy"

      - name: Stop services
        if: always()
        run: docker compose down -v

  # ==========================================
  # JOB 7: Deploy to Staging (only on main)
  # ==========================================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs:
      [frontend-unit-tests, frontend-e2e-tests, backend-tests, code-quality]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy notification
        run: |
          echo "Deploying to staging environment..."
          echo "This is where you'd deploy to your staging server"
          # Add your deployment commands here
          # Example: kubectl apply -f k8s/staging/
          # Or: docker-compose -f docker-compose.staging.yml up -d

  # ==========================================
  # JOB 8: Security Scan
  # ==========================================
  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-results.sarif"
