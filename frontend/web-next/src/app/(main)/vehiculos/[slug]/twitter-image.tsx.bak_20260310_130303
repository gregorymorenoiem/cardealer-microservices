/**
 * Twitter Card Image for Vehicle Detail Pages
 *
 * Re-exports the OpenGraph image for Twitter card compatibility.
 * Twitter uses summary_large_image card type with the same dimensions.
 */

export { default, runtime, size, contentType } from './opengraph-image';
export const alt = 'Vehículo en OKLA';
