{
  "$comment": "Docker environment configuration - secrets are injected via Docker secrets or environment variables",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "$comment": "Connection string is injected via secrets at runtime",
    "DefaultConnection": ""
  },
  "ServiceDiscovery": {
    "Enabled": true,
    "ServiceName": "marketingservice",
    "ServicePort": 80,
    "Consul": {
      "Host": "consul",
      "Port": 8500
    }
  },
  "HealthChecks": {
    "Enabled": true,
    "Path": "/health"
  }
}
